CREATE TABLE IF NOT EXISTS css_test_history (id INTEGER PRIMARY KEY AUTOINCREMENT, user_id INTEGER NOT NULL, test_date DATE NOT NULL, t200_seconds INTEGER NOT NULL, t400_seconds INTEGER NOT NULL, css_seconds INTEGER NOT NULL, css_pace_per_100m TEXT NOT NULL, source TEXT DEFAULT 'calculator', notes TEXT, created_at DATETIME DEFAULT CURRENT_TIMESTAMP, updated_at DATETIME DEFAULT CURRENT_TIMESTAMP, FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE);
CREATE INDEX IF NOT EXISTS idx_css_test_user_date ON css_test_history(user_id, test_date DESC);
CREATE TABLE IF NOT EXISTS swim_interval_history (id INTEGER PRIMARY KEY AUTOINCREMENT, user_id INTEGER NOT NULL, test_date DATE NOT NULL, css_seconds INTEGER NOT NULL, intervals_data TEXT NOT NULL, source TEXT DEFAULT 'calculator', notes TEXT, created_at DATETIME DEFAULT CURRENT_TIMESTAMP, updated_at DATETIME DEFAULT CURRENT_TIMESTAMP, FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE);
CREATE INDEX IF NOT EXISTS idx_swim_interval_user_date ON swim_interval_history(user_id, test_date DESC);
CREATE TABLE IF NOT EXISTS swim_cho_history (id INTEGER PRIMARY KEY AUTOINCREMENT, user_id INTEGER NOT NULL, test_date DATE NOT NULL, css_seconds INTEGER NOT NULL, fueling_data TEXT NOT NULL, source TEXT DEFAULT 'calculator', notes TEXT, created_at DATETIME DEFAULT CURRENT_TIMESTAMP, updated_at DATETIME DEFAULT CURRENT_TIMESTAMP, FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE);
CREATE INDEX IF NOT EXISTS idx_swim_cho_user_date ON swim_cho_history(user_id, test_date DESC);
CREATE TABLE IF NOT EXISTS cp_test_history (id INTEGER PRIMARY KEY AUTOINCREMENT, user_id INTEGER NOT NULL, test_date DATE NOT NULL, cp_watts INTEGER NOT NULL, w_prime_joules INTEGER NOT NULL, test_type TEXT NOT NULL, test_data TEXT NOT NULL, source TEXT DEFAULT 'calculator', notes TEXT, created_at DATETIME DEFAULT CURRENT_TIMESTAMP, updated_at DATETIME DEFAULT CURRENT_TIMESTAMP, FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE);
CREATE INDEX IF NOT EXISTS idx_cp_test_user_date ON cp_test_history(user_id, test_date DESC);
CREATE TABLE IF NOT EXISTS bike_zones_history (id INTEGER PRIMARY KEY AUTOINCREMENT, user_id INTEGER NOT NULL, test_date DATE NOT NULL, cp_watts INTEGER NOT NULL, zones_data TEXT NOT NULL, source TEXT DEFAULT 'calculator', notes TEXT, created_at DATETIME DEFAULT CURRENT_TIMESTAMP, updated_at DATETIME DEFAULT CURRENT_TIMESTAMP, FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE);
CREATE INDEX IF NOT EXISTS idx_bike_zones_user_date ON bike_zones_history(user_id, test_date DESC);
CREATE TABLE IF NOT EXISTS bike_vo2_history (id INTEGER PRIMARY KEY AUTOINCREMENT, user_id INTEGER NOT NULL, test_date DATE NOT NULL, cp_watts INTEGER NOT NULL, vo2_intervals TEXT NOT NULL, source TEXT DEFAULT 'calculator', notes TEXT, created_at DATETIME DEFAULT CURRENT_TIMESTAMP, updated_at DATETIME DEFAULT CURRENT_TIMESTAMP, FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE);
CREATE INDEX IF NOT EXISTS idx_bike_vo2_user_date ON bike_vo2_history(user_id, test_date DESC);
CREATE TABLE IF NOT EXISTS bike_best_effort_history (id INTEGER PRIMARY KEY AUTOINCREMENT, user_id INTEGER NOT NULL, test_date DATE NOT NULL, intervals_data TEXT NOT NULL, source TEXT DEFAULT 'calculator', notes TEXT, created_at DATETIME DEFAULT CURRENT_TIMESTAMP, updated_at DATETIME DEFAULT CURRENT_TIMESTAMP, FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE);
CREATE INDEX IF NOT EXISTS idx_bike_best_effort_user_date ON bike_best_effort_history(user_id, test_date DESC);
CREATE TABLE IF NOT EXISTS bike_low_cadence_history (id INTEGER PRIMARY KEY AUTOINCREMENT, user_id INTEGER NOT NULL, test_date DATE NOT NULL, cp_watts INTEGER NOT NULL, intervals_data TEXT NOT NULL, source TEXT DEFAULT 'calculator', notes TEXT, created_at DATETIME DEFAULT CURRENT_TIMESTAMP, updated_at DATETIME DEFAULT CURRENT_TIMESTAMP, FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE);
CREATE INDEX IF NOT EXISTS idx_bike_low_cadence_user_date ON bike_low_cadence_history(user_id, test_date DESC);
CREATE TABLE IF NOT EXISTS bike_cho_history (id INTEGER PRIMARY KEY AUTOINCREMENT, user_id INTEGER NOT NULL, test_date DATE NOT NULL, cp_watts INTEGER NOT NULL, fueling_data TEXT NOT NULL, source TEXT DEFAULT 'calculator', notes TEXT, created_at DATETIME DEFAULT CURRENT_TIMESTAMP, updated_at DATETIME DEFAULT CURRENT_TIMESTAMP, FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE);
CREATE INDEX IF NOT EXISTS idx_bike_cho_user_date ON bike_cho_history(user_id, test_date DESC);
CREATE TABLE IF NOT EXISTS bike_training_zones_history (id INTEGER PRIMARY KEY AUTOINCREMENT, user_id INTEGER NOT NULL, test_date DATE NOT NULL, cp_watts INTEGER NOT NULL, zones_data TEXT NOT NULL, source TEXT DEFAULT 'calculator', notes TEXT, created_at DATETIME DEFAULT CURRENT_TIMESTAMP, updated_at DATETIME DEFAULT CURRENT_TIMESTAMP, FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE);
CREATE INDEX IF NOT EXISTS idx_bike_training_zones_user_date ON bike_training_zones_history(user_id, test_date DESC);
CREATE TABLE IF NOT EXISTS bike_lt1_ogc_history (id INTEGER PRIMARY KEY AUTOINCREMENT, user_id INTEGER NOT NULL, test_date DATE NOT NULL, lt1_watts INTEGER NOT NULL, ogc_data TEXT NOT NULL, source TEXT DEFAULT 'calculator', notes TEXT, created_at DATETIME DEFAULT CURRENT_TIMESTAMP, updated_at DATETIME DEFAULT CURRENT_TIMESTAMP, FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE);
CREATE INDEX IF NOT EXISTS idx_bike_lt1_ogc_user_date ON bike_lt1_ogc_history(user_id, test_date DESC);
CREATE TABLE IF NOT EXISTS run_cs_history (id INTEGER PRIMARY KEY AUTOINCREMENT, user_id INTEGER NOT NULL, test_date DATE NOT NULL, cs_pace_seconds INTEGER NOT NULL, d_prime_meters INTEGER NOT NULL, test_type TEXT NOT NULL, test_data TEXT NOT NULL, source TEXT DEFAULT 'calculator', notes TEXT, created_at DATETIME DEFAULT CURRENT_TIMESTAMP, updated_at DATETIME DEFAULT CURRENT_TIMESTAMP, FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE);
CREATE INDEX IF NOT EXISTS idx_run_cs_user_date ON run_cs_history(user_id, test_date DESC);
CREATE TABLE IF NOT EXISTS run_best_effort_history (id INTEGER PRIMARY KEY AUTOINCREMENT, user_id INTEGER NOT NULL, test_date DATE NOT NULL, intervals_data TEXT NOT NULL, source TEXT DEFAULT 'calculator', notes TEXT, created_at DATETIME DEFAULT CURRENT_TIMESTAMP, updated_at DATETIME DEFAULT CURRENT_TIMESTAMP, FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE);
CREATE INDEX IF NOT EXISTS idx_run_best_effort_user_date ON run_best_effort_history(user_id, test_date DESC);
CREATE TABLE IF NOT EXISTS run_pace_zones_history (id INTEGER PRIMARY KEY AUTOINCREMENT, user_id INTEGER NOT NULL, test_date DATE NOT NULL, cs_pace_seconds INTEGER NOT NULL, zones_data TEXT NOT NULL, source TEXT DEFAULT 'calculator', notes TEXT, created_at DATETIME DEFAULT CURRENT_TIMESTAMP, updated_at DATETIME DEFAULT CURRENT_TIMESTAMP, FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE);
CREATE INDEX IF NOT EXISTS idx_run_pace_zones_user_date ON run_pace_zones_history(user_id, test_date DESC);
CREATE TABLE IF NOT EXISTS run_vo2_history (id INTEGER PRIMARY KEY AUTOINCREMENT, user_id INTEGER NOT NULL, test_date DATE NOT NULL, cs_pace_seconds INTEGER NOT NULL, vo2_intervals TEXT NOT NULL, source TEXT DEFAULT 'calculator', notes TEXT, created_at DATETIME DEFAULT CURRENT_TIMESTAMP, updated_at DATETIME DEFAULT CURRENT_TIMESTAMP, FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE);
CREATE INDEX IF NOT EXISTS idx_run_vo2_user_date ON run_vo2_history(user_id, test_date DESC);
CREATE TABLE IF NOT EXISTS run_cho_history (id INTEGER PRIMARY KEY AUTOINCREMENT, user_id INTEGER NOT NULL, test_date DATE NOT NULL, cs_pace_seconds INTEGER NOT NULL, fueling_data TEXT NOT NULL, source TEXT DEFAULT 'calculator', notes TEXT, created_at DATETIME DEFAULT CURRENT_TIMESTAMP, updated_at DATETIME DEFAULT CURRENT_TIMESTAMP, FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE);
CREATE INDEX IF NOT EXISTS idx_run_cho_user_date ON run_cho_history(user_id, test_date DESC);
CREATE TABLE IF NOT EXISTS run_training_zones_history (id INTEGER PRIMARY KEY AUTOINCREMENT, user_id INTEGER NOT NULL, test_date TEXT NOT NULL, cs_pace_seconds REAL, zones TEXT, test_data TEXT, source TEXT, notes TEXT, created_at DATETIME DEFAULT CURRENT_TIMESTAMP, FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE);
CREATE INDEX IF NOT EXISTS idx_run_training_zones_history_user_date ON run_training_zones_history(user_id, test_date DESC);
